transformers>=4.52.0
torch>=2.1.0
numpy>=1.24.0,<2.3.0
pillow>=10.0.0
safetensors>=0.4.0
requests>=2.31.0
pywebview>=5.0
pythonnet>=3.0.3 ; platform_system == "Windows"
pywin32>=306 ; platform_system == "Windows"
pypdfium2>=4.30.0
pypdf>=4.0.0
accelerate>=0.30.0
sentencepiece>=0.1.99
protobuf>=4.25.0
stanza>=1.8.0
PyYAML>=6.0
opencv-python-headless>=4.9.0
psutil>=5.9.0

# GPU extras live in requirements-gpu.txt (bitsandbytes, CUDA only).
# The app also installs it at runtime via core/quant_bootstrap.py.